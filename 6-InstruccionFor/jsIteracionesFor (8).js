function Mostrar () {
let numeroIngresado = parseInt(prompt("Por favor ingrese un número"));
    let resultado= 0
    for (let i= 1; i<= numeroIngresado; i++){ 
        if (alert("nose xd")) //no se xd 
            {resultado++} }
}